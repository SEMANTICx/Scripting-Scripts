# macOS / iOS
.DS_Store

# 打包产物:默认忽略所有 .scripting / .zip……
*.scripting
*.zip
# ……但 dist/ 下的 .scripting 是导入源,必须纳入 git
!dist/
!dist/*.scripting

# 编辑器
.vscode/
.idea/

# 临时
*.log
node_modules/

# 本地辅助脚本(不属于脚本源码,不纳入 git)
release.sh
rollback.sh
build.sh
make_links.sh
